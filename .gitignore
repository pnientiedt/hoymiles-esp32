.pio/

# Per-deployment secrets — copy secrets.example.h to secrets.h
src/secrets.h

# Local agent worktree/tooling directory
.claude/

# Serial-capture logs, one timestamped file per run
logs/

# BLE reference-library test harness: keep the scripts, ignore the local venv
ble-test/.venv/

# OS / editor cruft
.DS_Store
*.swp
